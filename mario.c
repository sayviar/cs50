#include <stdio.h>
#include <cs50.h>

int main(void)
{
    do {
    int n = get_int("How high should the pyramid be?\n");
    }
    while (n > 0 && n < 9);

    for (int i = 1; i <= n; i++)
    {
        print
    }
}
