#include <stdio.h>
#include <cs50.h>

int main(void)
{
    if (x < y)
    {
        printf("%i is less than %i\n");
    }
    else if (x > y)
    {
        printf("x is not less than y\n");
    }
    else if 
    {
        printf("x is equal to y\n");
    }
}
