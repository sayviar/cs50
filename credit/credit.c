#include <cs50.h>
#include <stdio.h>

int main(void)
{
    long cardnumber;
    do
    {
        cardnumber = get_long("Number: ");
    }
    while (False);


    for (int number = cardnumber / 10; number > 0; number / 100)
    {
        
    }
}
