#include <stdio.h>
#include <cs50.h>

int minimumCoins(int change);
int main(void)
{
    int change = get_int("Change owed: ");

}

int minimumCoins(int change)
{
    int coins = 0;
    while (change > 25)
    {
        coins++
        
    }
}
